# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Original sample scripts (kept for reference, not part of the project)
EPaper_AP_Portal/

# Server runtime data
server/data/

# Device config (contains credentials — use config_sample.h as template)
EPaper_Remote_Client/config.h
